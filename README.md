# Installing Package

``` bash
sudo apt update
sudo apt upgrade -y
sudo apt install python -y
sudo apt install git -y
sudo apt install root-repo -y
sudo apt install python3 python3-pip -y
sudo apt install aircrack-ng -y
pip install scapy

```
# Note [ ! ]
Only Root
